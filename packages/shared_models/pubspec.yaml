name: shared_models
description: Runtime session models (WorkoutSession, SetLog, ExerciseState, WatchPayload) for MennoTracker.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

resolution: workspace

dependencies:
  meta: ^1.15.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
