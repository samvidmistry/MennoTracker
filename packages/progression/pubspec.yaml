name: progression
description: Rep-range autoregulation progression engine for MennoTracker.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

resolution: workspace

dependencies:
  meta: ^1.15.0
  program:
    path: ../program
  shared_models:
    path: ../shared_models

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
